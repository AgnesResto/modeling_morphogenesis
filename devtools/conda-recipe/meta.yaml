package:
  name: modeling_morphogenesis
  version: "0.0.0"

source:
  path: ../..

build:
  number: 0

requirements:
  host:
    - python
    - setuptools

  run:
    - python

test:
  requires:
    - pytest
  imports:
    - modeling_morphogenesis

about:
  home: add_url_here
  license: MIT License
